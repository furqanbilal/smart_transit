import 'package:flutter/material.dart';

main() {
  runApp(MaterialApp(
    title: "STS",
    home: Scaffold(
      appBar: AppBar(
        title: Text("Smart Transit"),
        backgroundColor: Colors.amberAccent,
      ),
    ),
  ));
}
